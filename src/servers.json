{
  "servers": [
    {
      "url": "https://api.{region}.onelogin.com/api",
      "variables": {
        "region": {
          "enum": [
            "us",
            "eu"
          ],
          "default": "us",
          "description": "The location of the OneLogin instance"
        }
      }
    }
  ]
}